apiVersion: v1
kind: Service
metadata:
  name: pii-redactor
  namespace: poc-mcp-trust-plane
  labels:
    app: pii-redactor
    autopoc.io/project: mcp-trust-plane
spec:
  type: ClusterIP
  selector:
    app: pii-redactor
  ports:
    - port: 6002
      targetPort: 6002
