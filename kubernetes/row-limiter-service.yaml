apiVersion: v1
kind: Service
metadata:
  name: row-limiter
  namespace: poc-mcp-trust-plane
  labels:
    app: row-limiter
    autopoc.io/project: mcp-trust-plane
spec:
  type: ClusterIP
  selector:
    app: row-limiter
  ports:
    - port: 6003
      targetPort: 6003
