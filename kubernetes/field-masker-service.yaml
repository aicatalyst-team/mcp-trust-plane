apiVersion: v1
kind: Service
metadata:
  name: field-masker
  namespace: poc-mcp-trust-plane
  labels:
    app: field-masker
    autopoc.io/project: mcp-trust-plane
spec:
  type: ClusterIP
  selector:
    app: field-masker
  ports:
    - port: 6008
      targetPort: 6008
