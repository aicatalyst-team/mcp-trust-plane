apiVersion: v1
kind: Service
metadata:
  name: sql-guard
  namespace: poc-mcp-trust-plane
  labels:
    app: sql-guard
    autopoc.io/project: mcp-trust-plane
spec:
  type: ClusterIP
  selector:
    app: sql-guard
  ports:
    - port: 6001
      targetPort: 6001
